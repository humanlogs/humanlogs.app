-- Bug reports are feedback like the rest: same table, same admin panel, one more
-- type. Kept apart from FEATURE_REQUEST so "something is broken" never queues up
-- behind "it would be nice if".
ALTER TYPE "FeedbackType" ADD VALUE 'BUG_REPORT';
